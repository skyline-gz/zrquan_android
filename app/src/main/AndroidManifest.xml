<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zrquan.mobile" >

    <application
        android:name="ZrquanApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/StartUpTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name=".ui.MainActivity"/>
        <activity android:label="设置Demo" android:name=".ui.demo.DemoSettingActivity"/>
        <activity android:label="登录" android:name=".ui.UserLoginActivity"/>
        <activity android:label="注册" android:name=".ui.UserRegisterActivity"/>
    </application>

</manifest>
